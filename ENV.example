# Copy this file to `.env.local` and fill in values.
#
# Next.js loads `.env.local` automatically for local development.
# (Do NOT commit `.env.local`.)

# ----------------------------
# Firebase Admin (server-side)
# ----------------------------
# Recommended: set these 3 values from a Firebase service account JSON.
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=your-service-account-client-email

# IMPORTANT:
# Put the private key in quotes and keep `\n` escapes (do NOT paste as real newlines).
# Example:
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nABC...\n-----END PRIVATE KEY-----\n"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nREPLACE_ME\n-----END PRIVATE KEY-----\n"

# Alternative (recommended if you keep hitting "Invalid PEM formatted message"):
# Base64 encode the *entire* PEM key (including BEGIN/END lines and real newlines).
# Then set it here and leave FIREBASE_PRIVATE_KEY empty.
# FIREBASE_PRIVATE_KEY_BASE64=BASE64_ENCODED_PEM_HERE

# Optional (alternative configs):
# Provide a path (relative to repo root) to a service account JSON file.
# FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
#
# Or provide the full service account JSON as a string (must be valid JSON).
# FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# Optional: Override collection names
FIREBASE_USERS_COLLECTION=users
FIREBASE_POSTS_COLLECTION=artPosts
FIREBASE_DOCUMENTS_COLLECTION=documentation-submissions
FIREBASE_STATUS_CARDS_COLLECTION=status-cards
FIREBASE_NEWS_COLLECTION=news
FIREBASE_BUSINESSES_COLLECTION=businesses
FIREBASE_RESOURCES_COLLECTION=resources
FIREBASE_COMMUNITIES_COLLECTION=communities


